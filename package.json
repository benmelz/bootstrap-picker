{
  "name": "bootstrap-picker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "lint": "eslint",
    "lint:css": "stylelint **/*.css",
    "preview": "vite preview",
    "test": "vitest run --coverage --coverage.thresholds.100"
  },
  "devDependencies": {
    "@testing-library/dom": "*",
    "@vitest/browser": "*",
    "@vitest/coverage-istanbul": "*",
    "bootstrap": "^5.3.6",
    "eslint": "*",
    "neostandard": "*",
    "stylelint": "*",
    "stylelint-config-standard": "*",
    "vite": "*",
    "vitest": "*",
    "webdriverio": "*"
  }
}
